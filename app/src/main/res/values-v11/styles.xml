<resources>

    <!--
        Base application theme for API 11+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 11+ devices.
    -->
    <style name="AppBaseTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <style name="SettingsBaseTheme" parent="@style/Theme.AppCompat">
        <!-- API 11 theme customizations can go here. -->

        <item name="android:dialogTheme">@style/DialogWithoutBorders</item>
        <item name="android:alertDialogTheme">@style/AlertDialogWithoutBorders</item>
    </style>

    <style name="DialogWithoutBorders" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AlertDialogWithoutBorders" parent="@style/Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>